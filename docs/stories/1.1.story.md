---
storyId: 1.1
epicId: 1
title: "Website Analysis Engine - Core Infrastructure"
status: Draft
created: 2026-04-13
author: Orion (AIOS Master)
priority: P0
estimate: "13 points"

clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.1: Website Analysis Engine - Core Infrastructure

## Story Statement

**As a** Designer or Product Manager  
**I want to** analyze a website's visual design by providing its URL  
**So that** I can extract design system elements for documentation

## Description

This is the **foundational story** for the Design System Generator tool. It establishes the core web analysis infrastructure that all subsequent stories (color extraction, typography, spacing, etc.) will build upon.

### Core Responsibility

Build a robust web analysis engine that:
1. Accepts a website URL as input
2. Renders the website and extracts the DOM structure
3. Provides analysis APIs for downstream components
4. Handles errors gracefully (invalid URLs, timeouts, blocked content)
5. Logs analysis metadata for debugging

### What It Includes

✅ **Browser automation setup** (Puppeteer/Playwright)  
✅ **URL validation and normalization**  
✅ **DOM parsing and element extraction**  
✅ **Screenshot and visual capture**  
✅ **Error handling and retry logic**  
✅ **Analysis session management**  

### What It Does NOT Include

❌ Color extraction (Story 1.2)  
❌ Typography detection (Story 1.3)  
❌ Spacing/grid analysis (Story 1.4)  
❌ Report generation (Story 1.5)  

---

## Acceptance Criteria

### AC 1: Web Analysis Engine Initialization
- [ ] `AnalysisEngine` class initializes successfully
- [ ] Supports headless browser launch (Puppeteer/Playwright)
- [ ] Manages browser lifecycle (open, close, cleanup)
- [ ] Handles concurrent analysis sessions

**Source:** [Epic 1: Web Analysis Engine](#epic-overview)

### AC 2: Website Navigation & Rendering
- [ ] Accepts valid website URLs (http/https)
- [ ] Navigates to URL and waits for page load (with timeout)
- [ ] Validates page loaded successfully (status 200-299)
- [ ] Captures full-page screenshot
- [ ] Returns raw DOM to caller

**Source:** [Story 1.1: Core Responsibility](#core-responsibility)

### AC 3: URL Validation
- [ ] Rejects invalid URLs with clear error messages
- [ ] Normalizes URLs (adds protocol if missing)
- [ ] Handles redirects (follows up to 5 redirects)
- [ ] Detects infinite redirects and halts

**Validation Rules:**
- URL must contain valid domain
- Must not be localhost or 127.0.0.1
- Must not be private IP range (10.*, 172.16.*, 192.168.*)

**Source:** [AC 3: URL Validation](#acceptance-criteria)

### AC 4: DOM Element Extraction
- [ ] Extracts all visible DOM elements with:
  - Tag name
  - CSS computed styles (color, font, size, spacing)
  - Bounding box coordinates
  - Visibility state (visible/hidden/scroll)
- [ ] Handles deep nesting (no limit on depth)
- [ ] Filters out script/style/meta tags
- [ ] Returns structured element tree

**Source:** [AC 4: DOM Element Extraction](#acceptance-criteria)

### AC 5: Error Handling & Resilience
- [ ] Network timeout (> 30s) → Returns descriptive error
- [ ] Page crash/unresponsive → Retry logic (max 3 attempts)
- [ ] Missing elements → Graceful degradation (partial data OK)
- [ ] CORS/CSP blocks → Logs warning, continues analysis
- [ ] Memory limit exceeded → Automatic cleanup & halt

**Source:** [AC 5: Error Handling & Resilience](#acceptance-criteria)

### AC 6: Session Management
- [ ] Creates unique session ID for each analysis
- [ ] Stores analysis metadata (URL, timestamp, status, duration)
- [ ] Provides session lookup by ID
- [ ] Auto-cleanup of old sessions (>24h)

**Source:** [Story 1.1: What It Includes](#what-it-includes)

### AC 7: Performance Requirements
- [ ] First page load: < 15 seconds
- [ ] DOM extraction: < 5 seconds  
- [ ] Total analysis time: < 20 seconds per website
- [ ] Memory usage: < 200MB per session

**Source:** [Epic 1: Success Criteria](docs/prd/epic-1-design-system-generator.md#success-criteria)

---

## Scope

### IN SCOPE
- Browser automation setup (Puppeteer OR Playwright)
- URL validation and normalization
- Page load detection and timeout handling
- Full-page screenshot capture
- DOM extraction and element analysis
- Session management and cleanup
- Error handling and retry logic
- Unit tests for core functions
- Basic logging and debugging

### OUT OF SCOPE
- Color extraction or analysis
- Typography detection
- Spacing/grid measurement
- Report generation
- Design system documentation
- UI dashboard or API server (future story)

---

## Dependencies

### Prerequisite Stories
- None (this is Story 1.1 - no dependencies within Epic 1)

### External Dependencies
- Node.js 18+ (already installed ✅)
- npm/package manager (already installed ✅)
- npm packages to install:
  - `puppeteer` OR `playwright` (browser automation)
  - `cheerio` (DOM parsing alternative)
  - `axios` (HTTP requests, optional)
  - `node-fetch` (modern fetch API)

### Architectural Constraints
- Must use modern JavaScript (ES6+)
- Async/await pattern for browser operations
- Error handling via try-catch or Promise.catch()
- No synchronous operations (blocking)

---

## Dev Notes

### Previous Story Insights
N/A (First story of Epic 1)

### Analysis Engine Architecture

**File Structure:**
```
packages/web/
├── src/
│   ├── engine/
│   │   ├── AnalysisEngine.js       # Main engine class
│   │   ├── BrowserManager.js       # Browser lifecycle
│   │   ├── URLValidator.js         # URL validation
│   │   ├── DOMExtractor.js         # DOM parsing
│   │   └── SessionManager.js       # Session tracking
│   ├── utils/
│   │   ├── logger.js               # Debug logging
│   │   ├── errors.js               # Error definitions
│   │   └── helpers.js              # Utility functions
│   └── index.js                    # Main export
└── tests/
    ├── engine.test.js
    ├── browser.test.js
    └── urlValidator.test.js
```

[Source: Project Structure - packages/web]

### Technology Stack

| Component | Technology | Reasoning |
|-----------|-----------|-----------|
| Browser Automation | **Puppeteer** | Stable, feature-rich, handles headless rendering |
| DOM Parsing | Native DOM API | Browser-native, no extra dependencies |
| Async Runtime | Node.js EventLoop | Built-in, efficient |
| Error Handling | Custom error classes | Type-safe error management |
| Logging | Custom logger | Lightweight, no external dependencies |

[Source: Epic 1 Architecture Notes](docs/prd/epic-1-design-system-generator.md#architecture-notes)

### Implementation Approach

**Phase 1: Setup**
- Initialize npm project structure
- Install Puppeteer and dependencies
- Create error/logger utilities

**Phase 2: Core Engine**
- Implement `AnalysisEngine` class
- Implement `BrowserManager` (launch, close, pool)
- Add basic session management

**Phase 3: URL Handling**
- Implement URL validation
- Implement redirect following
- Error handling for invalid URLs

**Phase 4: DOM Extraction**
- Implement DOM element extraction
- Build element tree structure
- Add CSS style extraction

**Phase 5: Testing & Cleanup**
- Unit tests for all components
- Integration tests
- Browser cleanup (memory management)

### Data Models

**Session Object:**
```javascript
{
  sessionId: "sess_1234567890",
  url: "https://example.com",
  status: "analyzing|complete|error",
  startTime: "2026-04-13T11:30:00Z",
  endTime: "2026-04-13T11:30:15Z",
  duration: 15000,  // milliseconds
  elementCount: 1234,
  errorMessage: null,
  metadata: {
    userAgent: "...",
    viewport: { width: 1920, height: 1080 }
  }
}
```

**DOM Element Object:**
```javascript
{
  id: "elem_123",
  tagName: "div",
  classes: ["container", "main"],
  boundingBox: { x: 0, y: 0, width: 1920, height: 1080 },
  styles: {
    color: "#FF5733",
    fontSize: "16px",
    fontFamily: "Arial, sans-serif",
    backgroundColor: "#FFFFFF"
  },
  isVisible: true,
  children: [ /* nested elements */ ]
}
```

[Source: AC 4 - DOM Element Extraction]

### Testing Strategy

**Unit Tests:**
- URLValidator: valid/invalid URLs, redirects, edge cases
- BrowserManager: launch, close, error recovery
- DOMExtractor: element extraction, style parsing
- SessionManager: CRUD operations, cleanup

**Integration Tests:**
- End-to-end analysis: URL → DOM extraction
- Error scenarios: timeout, network failure
- Concurrent sessions: multiple parallel analyses

**Manual Testing:**
- Analyze real websites (example.com, github.com)
- Capture screenshots for manual review
- Verify accuracy of extracted elements

[Source: Testing Strategy - Story 1.1]

---

## Tasks / Subtasks

### Task 1: Setup Project Structure & Dependencies (AC 1)
- [ ] Initialize npm project in `packages/web/`
- [ ] Create directory structure (src/, tests/, etc.)
- [ ] Install Puppeteer and core dependencies
- [ ] Create `.npmrc` and package.json scripts
- [ ] Setup test framework (Jest)
- **Unit Tests:** npm install validation

### Task 2: Implement AnalysisEngine Core Class (AC 1, AC 2)
- [ ] Create `AnalysisEngine` class with constructor
- [ ] Implement `analyze(url)` method (public API)
- [ ] Return session object with sessionId
- [ ] Basic error handling skeleton
- **Unit Tests:** Engine initialization, analyze method signature

### Task 3: Implement BrowserManager (AC 1, AC 2)
- [ ] Create `BrowserManager` class
- [ ] Implement `launch()` - start headless Puppeteer
- [ ] Implement `navigate(url, options)` - go to URL, wait for load
- [ ] Implement `takeScreenshot()` - capture page image
- [ ] Implement `close()` - cleanup browser resources
- **Unit Tests:** Browser lifecycle, page navigation, timeout handling

### Task 4: Implement URL Validator (AC 3)
- [ ] Create `URLValidator` class
- [ ] Implement `isValid(url)` - check URL format
- [ ] Implement `normalize(url)` - add protocol, clean up
- [ ] Implement `isPrivateIP(url)` - reject localhost/private ranges
- [ ] Implement `followRedirects(url)` - handle 301/302 chains
- **Unit Tests:** All validation rules, edge cases (RFC 3986)

### Task 5: Implement DOM Extractor (AC 4)
- [ ] Create `DOMExtractor` class
- [ ] Implement `extract(page)` - get all DOM elements
- [ ] Parse computed CSS styles for each element
- [ ] Build element tree structure (parent-child relationships)
- [ ] Filter script/style/meta tags
- [ ] Return structured element array
- **Unit Tests:** Element extraction, style parsing, filtering

### Task 6: Implement Session Manager (AC 6)
- [ ] Create `SessionManager` class
- [ ] Implement `createSession(url)` - new session with metadata
- [ ] Implement `getSession(sessionId)` - lookup session
- [ ] Implement `updateSessionStatus(sessionId, status)` - track progress
- [ ] Implement `cleanupOldSessions()` - remove >24h old sessions
- [ ] Implement `listActiveSessions()` - debugging
- **Unit Tests:** CRUD operations, cleanup logic

### Task 7: Error Handling & Resilience (AC 5)
- [ ] Create custom error classes (NetworkError, TimeoutError, ValidationError, etc.)
- [ ] Implement retry logic (max 3 attempts, exponential backoff)
- [ ] Implement timeout handler (>30s = error)
- [ ] Implement memory monitoring (halt if >200MB)
- [ ] Add graceful degradation (partial data OK)
- [ ] Comprehensive error logging
- **Unit Tests:** All error scenarios, retry logic

### Task 8: Logging & Debugging (AC 1)
- [ ] Create logger utility (info, warn, error levels)
- [ ] Add logging to all main operations
- [ ] Store analysis metadata (timestamp, duration, element count)
- [ ] Output debug info to `.aios/logs/analysis-engine.log`
- [ ] Support verbosity levels (--debug flag)
- **Unit Tests:** Logger functionality

### Task 9: Integration Testing (AC 2-7)
- [ ] Create integration test suite
- [ ] Test full flow: URL → navigation → DOM extraction
- [ ] Test with real websites (2-3 public sites)
- [ ] Test error scenarios (timeout, invalid URL, network fail)
- [ ] Test concurrent sessions (parallel analyses)
- [ ] Performance benchmarking: measure duration vs AC 7
- **Manual Testing:** Run against real websites, visual verification

### Task 10: Documentation & Code Review (AC 1-7)
- [ ] Write JSDoc comments for all public methods
- [ ] Create README.md for Analysis Engine module
- [ ] Document API and usage examples
- [ ] Code review via @qa (style, patterns, security)
- [ ] Run linting: `npm run lint`
- [ ] Run type checking if using JSDoc
- [ ] Update File List in this story
- **Unit Tests:** All tests passing, linting clean

---

## Criteria of Done (Definition of Done)

✅ **All Acceptance Criteria met** (AC 1-7)  
✅ **All Unit Tests passing** (100% coverage for core modules)  
✅ **Integration tests passing** (full flow working)  
✅ **Performance benchmarks met** (< 20 seconds per website)  
✅ **Linting passes** (`npm run lint`)  
✅ **Type checking passes** (JSDoc or TypeScript)  
✅ **No console errors** in test output  
✅ **Code reviewed** by @qa team  
✅ **Documentation complete** (README, JSDoc, API docs)  
✅ **No breaking changes** to architecture (none yet, greenfield)  
✅ **File List updated** below

---

## File List

### Files Created
- [ ] `packages/web/package.json` - Project manifest
- [ ] `packages/web/src/engine/AnalysisEngine.js` - Main engine
- [ ] `packages/web/src/engine/BrowserManager.js` - Browser lifecycle
- [ ] `packages/web/src/engine/URLValidator.js` - URL validation
- [ ] `packages/web/src/engine/DOMExtractor.js` - DOM parsing
- [ ] `packages/web/src/engine/SessionManager.js` - Session tracking
- [ ] `packages/web/src/utils/logger.js` - Logging utility
- [ ] `packages/web/src/utils/errors.js` - Error definitions
- [ ] `packages/web/src/index.js` - Main export
- [ ] `packages/web/tests/engine.test.js` - Engine tests
- [ ] `packages/web/tests/browser.test.js` - Browser tests
- [ ] `packages/web/tests/urlValidator.test.js` - URL validation tests
- [ ] `packages/web/tests/integration.test.js` - Integration tests
- [ ] `packages/web/README.md` - Module documentation

### Files Modified
- [ ] `core-config.yaml` - Add analysis engine configuration (if needed)

---

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type:** Backend/Infrastructure  
**Secondary Type:** Testing  
**Complexity:** Medium (new infrastructure, but single-purpose module)  

### Specialized Agent Assignment

**Primary Agents:**
- `@dev` (implementation)
- `@architect` (architecture decisions for extensibility)

**Supporting Agents:**
- `@qa` (test coverage validation)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `npm run lint && npm test` before marking tasks complete
- [ ] Pre-PR (@devops): Run CodeRabbit review before creating pull request

### CodeRabbit Focus Areas

**Primary Focus:**
- Async/await patterns (no callback hell)
- Error handling completeness (all paths covered)
- Memory management (browser cleanup, session cleanup)
- Security: URL validation (no SSRF, no localhost access)

**Secondary Focus:**
- Test coverage (aim for >80% for core modules)
- Performance (meet AC 7 timing requirements)
- Code reusability (clean interfaces for Story 1.2+)

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutes
- Severity Filter: CRITICAL only

---

## Change Log

- **2026-04-13:** Story created as foundation for Design System Generator
- **2026-04-13:** Acceptance criteria defined based on Epic 1 requirements
- **2026-04-13:** Task breakdown completed for 10-task implementation sprint

---

## Next Steps

After this story is **Done**:
1. @dev will implement all 10 tasks
2. @qa will validate acceptance criteria
3. Story 1.2 (Color Extraction) will begin (depends on this story's core engine)
4. Estimated completion: 2026-04-20

---

**Ready for Development!** ✅
