---
storyId: 1.2
epicId: 1
title: "Color Palette Extraction & Documentation"
status: Draft
created: 2026-04-13
author: River (Scrum Master)
priority: P0
estimate: "8 points"
dependsOn: ["1.1"]

clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.2: Color Palette Extraction & Documentation

## Story Statement

**As a** Designer  
**I want to** receive a structured color palette extracted from a website  
**So that** I can document and replicate the brand's color system in a Design System

## Description

Building on the foundation of Story 1.1 (Analysis Engine), this story deepens and professionalizes the color extraction capability. The current `DOMExtractor.extractColorPalette()` returns a flat list of raw CSS color strings — insufficient for a Designer's needs.

Story 1.2 transforms raw color data into a **structured, categorized color palette** with:
- Color normalization (CSS strings → HEX/RGB/HSL)
- Semantic categorization (primary, secondary, accent, neutral, background, text)
- Frequency-based ranking (most-used colors = most important)
- Contrast ratio analysis (accessibility compliance — WCAG 2.1)
- Named color matching (e.g., `rgb(255,0,0)` → `Red`)

### What It Includes

✅ **Color normalization** — CSS color strings → HEX + RGB + HSL  
✅ **Frequency analysis** — rank colors by usage across DOM elements  
✅ **Semantic categorization** — auto-classify into design roles  
✅ **Contrast ratio** — WCAG 2.1 AA/AAA compliance checks  
✅ **Named color matching** — map to nearest named color  
✅ **Palette deduplication** — merge similar colors (tolerance ± 5%)  
✅ **Color documentation output** — structured object ready for report (Story 1.5)  

### What It Does NOT Include

❌ Typography extraction (Story 1.3)  
❌ Spacing/grid analysis (Story 1.4)  
❌ Visual report rendering (Story 1.5)  
❌ UI to display the palette  

---

## Acceptance Criteria

### AC 1: Color Normalization
- [ ] Converts all CSS color formats to HEX, RGB, and HSL
  - Input: `rgb(255, 87, 51)`, `#FF5733`, `rgba(255,87,51,0.9)`, `hsl(11, 100%, 60%)`, named colors (`red`, `blue`)
  - Output per color: `{ hex: "#FF5733", rgb: "rgb(255,87,51)", hsl: "hsl(11,100%,60%)" }`
- [ ] Handles invalid/unparseable colors gracefully (skip + log warning)
- [ ] Strips alpha channel for solid color comparison (transparent colors noted separately)

### AC 2: Frequency Analysis & Ranking
- [ ] Counts how many DOM elements use each color (background + foreground)
- [ ] Returns colors sorted by frequency (most used = index 0)
- [ ] Minimum frequency threshold: colors used in < 2 elements filtered as noise
- [ ] Returns top-N colors (default: 20, configurable)

### AC 3: Semantic Color Categorization
- [ ] Automatically assigns one of: `primary`, `secondary`, `accent`, `neutral`, `background`, `text`, `border`, `unknown`
- [ ] Categorization rules (heuristic-based):
  - `background`: used as `backgroundColor` on `body`, `html`, `main`, `section` — typically light
  - `text`: used as `color` on `p`, `h1-h6`, `span` — typically dark
  - `primary`: most-used non-neutral non-background non-text color
  - `secondary`: 2nd most-used distinctive color
  - `accent`: high-saturation colors used sparingly (< 10% of elements)
  - `neutral`: near-grayscale colors (saturation < 10%)
  - `border`: colors used exclusively in `border` CSS properties
- [ ] Each color tagged with `role: "primary" | "secondary" | ...`

### AC 4: Contrast Ratio Analysis (WCAG 2.1)
- [ ] Calculates contrast ratio between:
  - Every `text` color vs every `background` color pair found on same element
  - Primary color vs white and black
- [ ] Tags each pair with compliance level:
  - `AAA`: ratio ≥ 7:1
  - `AA`: ratio ≥ 4.5:1 (normal text) / ≥ 3:1 (large text)
  - `FAIL`: ratio < 3:1
- [ ] Returns list of failing pairs with recommendation

### AC 5: Color Deduplication (Similarity Merging)
- [ ] Merges colors within ± 5% tolerance in HSL space
  - e.g., `#FF5733` and `#FF5A35` → merged as one color (most frequent variant kept)
- [ ] Returns both the canonical color and list of merged variants
- [ ] Configurable tolerance (default: 5%, range: 1–20%)

### AC 6: Named Color Matching
- [ ] Maps each extracted color to the nearest CSS named color
  - e.g., `rgb(255,87,51)` → `{ namedColor: "tomato", distance: 3.2 }`
- [ ] Uses Euclidean distance in RGB space for nearest-neighbor match
- [ ] Named color list: full CSS4 named colors (147 colors)

### AC 7: Structured Palette Output
- [ ] Returns structured palette object:
  ```javascript
  {
    extractedAt: "2026-04-13T...",
    url: "https://example.com",
    totalColorsFound: 89,
    uniqueColorsAfterDedup: 24,
    palette: [
      {
        id: "color_001",
        hex: "#1A1A2E",
        rgb: "rgb(26,26,46)",
        hsl: "hsl(240,28%,14%)",
        role: "primary",
        frequency: 142,
        namedColor: "midnight blue",
        wcag: {
          vsWhite: { ratio: 14.5, level: "AAA" },
          vsBlack: { ratio: 1.4, level: "FAIL" }
        },
        variants: ["#1B1B2F", "#191929"]
      }
    ],
    contrastFailures: [
      { text: "#888888", background: "#AAAAAA", ratio: 1.9, level: "FAIL" }
    ],
    accessibilityScore: 87  // percentage of text/bg pairs passing AA
  }
  ```
- [ ] Output is serializable (JSON-safe, no circular refs)
- [ ] `DOMExtractor.extractColorPalette()` updated to return this new structure

### AC 8: Integration with AnalysisEngine
- [ ] `AnalysisEngine.analyze()` returns the enhanced color palette (not the old flat array)
- [ ] Backward compatible: old consumers receive `results.colorPalette` with new structure
- [ ] No regression in Story 1.1 tests

---

## Scope

### IN SCOPE
- Color normalization module (CSS → HEX/RGB/HSL)
- Frequency analysis from extracted DOM elements
- Semantic role categorization (heuristic)
- WCAG 2.1 contrast ratio calculation
- Similarity-based deduplication
- Named color matching (CSS4 color list)
- Structured output object
- Unit tests for all new modules
- Update `DOMExtractor.extractColorPalette()` to use new pipeline

### OUT OF SCOPE
- Visual rendering of palette (Story 1.5)
- Custom color naming / brand color overrides (future)
- Gradient extraction
- Image/canvas color analysis
- Full color accessibility audit report (Story 1.5)

---

## Dependencies

### Prerequisite Stories
- **Story 1.1** ✅ Done — `DOMExtractor`, `AnalysisEngine`, element array with `styles.color` and `styles.backgroundColor`

### External Dependencies
- **No new npm packages required** — color math in pure JS
  - WCAG contrast formula: documented in W3C spec, implementable natively
  - CSS named colors: static JSON list (included in source)
  - HSL conversion: pure math

### Architectural Constraints
- Must extend `DOMExtractor` — not replace it
- New module: `packages/web/src/extractors/ColorExtractor.js`
- Follow existing patterns: class-based, JSDoc, custom errors, logger
- Input: array of element objects from Story 1.1 (`el.styles.color`, `el.styles.backgroundColor`, `el.tagName`, `el.classes`)

---

## Dev Notes

### Previous Story Insights (from Story 1.1)
- `DOMExtractor.extractColorPalette()` currently returns `{ colors: string[], count: number }` — flat, no metadata
- `AnalysisEngine.analyze()` already calls `extractColorPalette(elements)` and returns result as `colorPalette`
- Element object structure: `{ tagName, classes, styles: { color, backgroundColor, ... } }`
- Logger, custom errors, session management all available for reuse

[Source: docs/stories/1.1.story.md — Dev Agent Record]

### New Module Architecture

```
packages/web/src/
├── engine/
│   └── DOMExtractor.js        ← UPDATE extractColorPalette() to call ColorExtractor
└── extractors/
    └── ColorExtractor.js      ← NEW: full color pipeline
        ├── normalize(cssColor) → { hex, rgb, hsl }
        ├── analyzeFrequency(elements) → colorMap with counts
        ├── categorize(colorMap) → roles assigned
        ├── calculateContrast(color1, color2) → ratio + level
        ├── deduplicate(colors, tolerance) → merged palette
        ├── matchNamed(hex) → nearest CSS4 named color
        └── extract(elements) → full structured palette
```

### Color Math Reference

**HEX → RGB:**
```javascript
hex.match(/[A-F0-9]{2}/gi).map(v => parseInt(v, 16))
```

**RGB → HSL:**
```javascript
// Standard formula (r,g,b in 0-1 range)
const max = Math.max(r, g, b), min = Math.min(r, g, b);
const l = (max + min) / 2;
const s = max === min ? 0 : l > 0.5
  ? (max - min) / (2 - max - min)
  : (max - min) / (max + min);
```

**WCAG Contrast Ratio:**
```javascript
// Relative luminance L = 0.2126R + 0.7152G + 0.0722B
// Each channel: c <= 0.04045 ? c/12.92 : ((c+0.055)/1.055)^2.4
// Ratio = (L1 + 0.05) / (L2 + 0.05)  where L1 > L2
```

**CSS → parseable:**
- `rgb(r,g,b)` → direct parse
- `rgba(r,g,b,a)` → parse, note alpha
- `#RRGGBB` / `#RGB` → hex parse
- `hsl(h,s%,l%)` → convert to RGB
- Named colors → lookup in CSS4 table

[Source: W3C CSS Color Level 4 / WCAG 2.1 spec]

### File Locations

```
packages/web/src/extractors/ColorExtractor.js   ← new
packages/web/src/engine/DOMExtractor.js          ← update extractColorPalette()
packages/web/tests/colorExtractor.test.js        ← new
packages/web/tests/contrastRatio.test.js         ← new
```

### Data: CSS4 Named Colors

Include as static constant in `ColorExtractor.js`:
```javascript
const CSS4_NAMED_COLORS = {
  aliceblue: '#F0F8FF',
  antiquewhite: '#FAEBD7',
  // ... all 147 CSS4 named colors
};
```

---

## Tasks / Subtasks

### Task 1: Create ColorExtractor Module — Normalization (AC 1)
- [ ] Create `packages/web/src/extractors/` directory
- [ ] Create `ColorExtractor.js` class skeleton
- [ ] Implement `normalize(cssColor)` → `{ hex, rgb, hsl }`
  - [ ] Handle `rgb(r,g,b)` format
  - [ ] Handle `rgba(r,g,b,a)` format — strip alpha, flag as transparent
  - [ ] Handle `#RRGGBB` and `#RGB` formats
  - [ ] Handle `hsl(h,s%,l%)` format
  - [ ] Handle CSS4 named colors via lookup table
  - [ ] Handle invalid/unknown → return null, log warning
- [ ] Include full CSS4 named colors map (147 colors) as constant
- **Unit Tests:** normalize() with all format variations, edge cases, invalids

### Task 2: Frequency Analysis (AC 2)
- [ ] Implement `analyzeFrequency(elements)` in `ColorExtractor`
  - [ ] Count `styles.color` occurrences per element
  - [ ] Count `styles.backgroundColor` occurrences per element
  - [ ] Build frequency map: `{ normalizedHex: { count, sources: ['color'|'backgroundColor'] } }`
  - [ ] Filter noise: remove colors with count < 2
  - [ ] Sort by frequency descending
  - [ ] Enforce top-N cap (default 20, configurable)
- **Unit Tests:** frequency counting, noise filtering, sorting, top-N limit

### Task 3: Semantic Role Categorization (AC 3)
- [ ] Implement `categorize(frequencyMap, elements)` in `ColorExtractor`
  - [ ] Detect background colors from `body`, `html`, `main`, `section`, `div` `backgroundColor`
  - [ ] Detect text colors from `p`, `h1`–`h6`, `span`, `a` `color`
  - [ ] Assign `neutral` to near-grayscale (HSL saturation < 10%)
  - [ ] Assign `primary` to highest-frequency non-neutral/text/bg
  - [ ] Assign `secondary` to 2nd highest distinctive color
  - [ ] Assign `accent` to high-saturation (> 70%) low-frequency (< 10%)
  - [ ] Assign `border` to colors found only in border properties
  - [ ] Default unclassified to `unknown`
- **Unit Tests:** each role detection rule, multi-role scenarios

### Task 4: WCAG Contrast Ratio (AC 4)
- [ ] Implement `calculateLuminance(hex)` — WCAG relative luminance formula
- [ ] Implement `calculateContrast(hex1, hex2)` → `{ ratio, level }`
  - [ ] Level: `AAA` (≥7:1), `AA` (≥4.5:1), `AA-large` (≥3:1), `FAIL` (<3:1)
- [ ] Implement `analyzeContrast(palette)` — find all text/bg pairs from DOM context
  - [ ] Check each text color vs its element's background color
  - [ ] Check primary vs white and black
  - [ ] Collect failing pairs into `contrastFailures[]`
  - [ ] Calculate `accessibilityScore` (% passing AA)
- **Unit Tests:** luminance formula, contrast ratios (known values), level thresholds

### Task 5: Color Deduplication (AC 5)
- [ ] Implement `hexToHsl(hex)` helper
- [ ] Implement `colorDistance(hsl1, hsl2)` — Euclidean in HSL space
- [ ] Implement `deduplicate(colors, tolerance = 5)`:
  - [ ] Group colors within tolerance distance
  - [ ] Keep highest-frequency variant as canonical
  - [ ] Record merged variants in `variants[]`
- **Unit Tests:** similar color detection, merge behavior, tolerance boundary cases

### Task 6: Named Color Matching (AC 6)
- [ ] Add complete CSS4 named colors map (147 entries)
- [ ] Implement `matchNamed(hex)` → `{ namedColor, distance }`
  - [ ] Convert both to RGB
  - [ ] Calculate Euclidean distance in RGB space
  - [ ] Return closest match
- **Unit Tests:** exact matches, near matches, boundary cases

### Task 7: Structured Output Assembly (AC 7)
- [ ] Implement `extract(elements, url)` — main pipeline method:
  1. `analyzeFrequency(elements)` → raw color map
  2. `deduplicate()` → merged colors
  3. `normalize()` each canonical color → HEX/RGB/HSL
  4. `categorize()` → assign roles
  5. `matchNamed()` → add named color
  6. `analyzeContrast()` → WCAG pairs + failures
  7. Assemble and return structured palette object (AC 7 schema)
- [ ] Ensure output is JSON-serializable
- **Unit Tests:** full pipeline with mock element array, output schema validation

### Task 8: Update DOMExtractor Integration (AC 8)
- [ ] Update `DOMExtractor.extractColorPalette(elements)`:
  - [ ] Instantiate `ColorExtractor`
  - [ ] Delegate to `colorExtractor.extract(elements)`
  - [ ] Return new structured palette
- [ ] Verify `AnalysisEngine.analyze()` still works (no breaking change)
- [ ] Run full regression: `npm test` — all 52 existing tests still pass
- **Unit Tests:** DOMExtractor integration, AnalysisEngine no regression

### Task 9: Documentation & Cleanup (AC 1-8)
- [ ] JSDoc on all public methods in `ColorExtractor`
- [ ] Update `README.md` — document new palette output structure
- [ ] Update story File List
- [ ] Run `npm run lint` — 0 errors
- [ ] Final test run — all tests passing
- **Unit Tests:** full suite green

---

## Criteria of Done (Definition of Done)

✅ All ACs met (AC 1–8)  
✅ All unit tests passing (`npm test`)  
✅ No regression in Story 1.1 tests (52 tests still green)  
✅ Linting passes (`npm run lint`)  
✅ `AnalysisEngine.analyze()` returns enhanced `colorPalette`  
✅ Output is JSON-serializable  
✅ WCAG contrast analysis implemented and tested  
✅ JSDoc on all public methods  
✅ README updated  
✅ File List complete  

---

## File List

### Files Created
- [ ] `packages/web/src/extractors/ColorExtractor.js` — full color pipeline
- [ ] `packages/web/tests/colorExtractor.test.js` — unit tests
- [ ] `packages/web/tests/contrastRatio.test.js` — WCAG contrast tests

### Files Modified
- [ ] `packages/web/src/engine/DOMExtractor.js` — update `extractColorPalette()`
- [ ] `packages/web/README.md` — document new color palette output

---

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type:** Backend/Infrastructure  
**Secondary Type:** Algorithm/Data Processing  
**Complexity:** Medium-High (color math, WCAG spec, dedup algorithm)

### Specialized Agent Assignment

**Primary Agents:**
- `@dev` (implementation)

**Supporting Agents:**
- `@qa` (algorithm correctness — color math, WCAG formula)
- `@architect` (extensibility for Stories 1.3–1.5)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `npm run lint && npm test` — all tests green
- [ ] Pre-PR (@devops): CodeRabbit review — focus on color math correctness

### CodeRabbit Focus Areas

**Primary Focus:**
- Color math accuracy (HEX/RGB/HSL conversions)
- WCAG formula compliance (W3C spec adherence)
- Deduplication correctness (tolerance merging)

**Secondary Focus:**
- No floating point precision issues
- Edge cases: transparent colors, empty palette, all-white/black sites
- Performance: large element arrays (> 5000 elements)

### Self-Healing Configuration
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutes
- Severity Filter: CRITICAL only

---

## Change Log

- **2026-04-13:** Story 1.2 created by @sm (River) after Story 1.1 marked Done
- **2026-04-13:** Scope defined from Epic 1 — Color Palette Extraction & Documentation
- **2026-04-13:** Dev Notes populated from Story 1.1 implementation context
